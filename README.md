# Cloud for AI – Mushroom Classification

This project contains a complete Cloud for AI pipeline.

## Structure

- `app/`  
  Docker container hosting the prediction web application.

- `training/`  
  Docker container used to train the machine learning model.

- `docker-compose.yaml`  
  Used to run the training and application containers locally.


